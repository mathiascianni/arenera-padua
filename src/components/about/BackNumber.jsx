const BackNumber = ({ number }) => {
    return (
        <div className="text-xl font-anton absolute -top-4 -right-0 lg:-top-6 lg:-left-6 bg-primary text-white aspect-square flex items-center justify-center rounded-full w-12 opacity-30 select-none">{number}</div>
    )
}

export default BackNumber